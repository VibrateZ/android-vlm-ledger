# HyperOS 离线自动记账应用开发计划

> 本文档是后续开发和 AI 协作的主约束。任何涉及权限、识别来源、数据存储、自动入账阈值或联网能力的调整，都必须先更新本文档并说明风险。

## 1. 产品目标与范围

开发一款个人侧载、源码公开的 Android 记账应用，优先适配小米 HyperOS，同时尽量兼容其他 Android 设备。

首版目标：

- 自动识别微信和支付宝的支出、收入及退款。
- 支持 HyperOS 三指截屏后自动发现新截图，并在本地完成页面分类和 OCR。
- 仅将高置信度、具有明确成功状态且满足新鲜度条件的结果自动入账；其他结果进入待确认列表。
- 识别完成后显示一次性结果浮层；允许修改标签、保留截图或请求系统删除截图。
- 按固定月预算计算每日参考预算和当日结余/超支；不动态调整后续日期额度。
- 标签只用于分类统计和展示，不参与每日预算计算。
- 支持微信、支付宝官方账单导入，用于补漏、纠错、去重和最终对账。
- 一键导出 CSV 与 XLSX 月报。
- 应用完全离线运行，不声明 `INTERNET` 权限，不接入账号、遥测或云同步。

不在首版实现：

- 读取短信、联系人、剪贴板或全部通知内容的通用识别。
- 持续遍历微信、支付宝的无障碍控件树。
- MediaProjection 常驻录屏、Root、Shizuku 或绕过 `FLAG_SECURE`。
- 自动操作支付按钮、输入密码或通过 `dispatchGesture()` 模拟付款操作。
- 应用商店上架和多设备云同步。

## 2. 技术栈与工程结构

- Kotlin、Gradle Kotlin DSL、Jetpack Compose、Material 3、Coroutines/Flow。
- `minSdk 26`；`compileSdk` 和 `targetSdk` 使用建项时最新稳定版本。
- 首版采用单 `app` 模块，按 `ledger`、`detection`、`screenshot`、`overlay`、`reconciliation`、`report`、`security`、`ui` 分包。
- Room 保存账本；金额统一使用 `Long` 表示最小货币单位，禁止使用浮点数处理金额。
- SQLCipher 加密数据库；随机数据库密钥由 Android Keystore 的 AES-GCM 密钥封装。
- DataStore 仅保存非敏感设置；关闭 Android 自动备份和设备间数据迁移。
- ML Kit 中文文本识别使用可离线打包的模型，不能在首次使用时联网下载。
- CSV 使用成熟的结构化读写库；XLSX 使用流式生成库，避免大月报占用过多内存。
- 导入、对账、OCR 和报表生成在后台协程或 WorkManager 中执行，不阻塞 UI。
- 文件选择与导出统一使用 Storage Access Framework。

核心接口：

```kotlin
interface ScreenshotDetector {
    fun events(): Flow<ScreenshotCandidate>
}

interface PaymentPageClassifier {
    suspend fun classify(image: InputImage): PageClassification
}

interface PaymentExtractor {
    suspend fun extract(image: InputImage, classification: PageClassification): PaymentCandidate
}

interface StatementImporter {
    suspend fun import(uri: Uri): ImportResult
}
```

`ScreenshotDetector` 必须允许 HyperOS 实现和通用 Android 后备实现独立替换；识别规则不得散落在 UI 或 Service 中。

## 3. 截图发现与按需识别

### 3.1 触发路径

首选路径：

1. 用户通过 HyperOS 三指手势生成系统截图。
2. `NotificationListenerService` 观察 SystemUI 的截图完成通知，仅将其作为“可能出现新截图”的触发信号。
3. 应用在有限时间窗口内查询 MediaStore，寻找已写入完成的新截图。
4. 找到唯一候选后执行页面分类和本地 OCR。

兼容后备：

- 在系统已绑定的服务存活期间注册 `MediaStore ContentObserver`，只响应新增图片并立即过滤非截图候选。
- 保留快捷设置磁贴“识别最近截图”，供 SystemUI 通知格式变化、后台被限制或自动发现失败时手动触发。
- 保留系统分享入口作为最低兼容路径，但不作为主要交互。

权限约束：

- Android 13+ 自动读取未来截图需要用户授予完整 `READ_MEDIA_IMAGES` 权限；“仅选择部分照片”不能保证自动识别。
- 首次启用时明确说明该权限技术上可以读取全部图片，但实现只查询刚生成的截图候选。
- 用户拒绝完整图片权限时，自动发现关闭，快捷磁贴只能提示改用系统分享入口。
- 不使用 UsageStats 作为付款确认来源；它只能提供应用使用事件，不能读取金额或证明支付成功。

### 3.2 截图候选过滤

候选图片必须同时满足：

- MediaStore 创建/修改时间位于触发窗口内。
- `IS_PENDING = 0`，文件可完整读取。
- 路径、显示名称或 MIME 类型符合当前设备已验证的截图特征。
- 尺寸和方向符合屏幕截图的合理范围。
- URI、文件大小和内容哈希未处理过。

SystemUI 通知包名、频道、文件命名和截图目录必须做成版本化设备适配配置，不能硬编码为唯一值。查询失败时使用短暂、有限次数的退避重试，禁止持续轮询图库。

## 4. 支付成功页面判定

识别必须分为“页面分类”和“字段提取”两个阶段。OCR 发现“支付成功”四个字本身绝不构成自动入账条件。

### 4.1 三道识别门槛

1. **正特征**：平台标志、明确的成功状态、金额区域、商户/对方区域以及符合该版本的相对布局。
2. **负特征**：账单列表、交易记录、月度账单、聊天消息、图片预览、账单分享海报、搜索结果、支付详情历史页等标志。
3. **新鲜度**：页面显示的交易时间接近截图时间，或截图与近期同平台、同金额的支付通知匹配。

只要命中强负特征，或者无法证明新鲜度，就不得自动入账。它可以显示识别结果供用户确认，但不能把截图生成时间伪装成精确交易时间。

### 4.2 特征表达

- 首版以 ML Kit OCR 文本块、标准化坐标、文字邻接关系和少量稳定图标模板为主，不先训练自定义图像模型。
- 微信和支付宝分别维护 `PageFingerprint`，并按应用版本、主题和页面类型版本化。
- 规则包含必需锚点、可选锚点、排除锚点、金额区域、时间区域和置信度权重。
- 颜色只能作为弱特征；不能因深色模式、护眼模式、字体缩放或屏幕尺寸变化而直接失败。
- 图标模板匹配只能辅助 OCR，不能单独证明支付完成。
- 每次规则变更必须增加相应正样本、负样本和回归测试。

### 4.3 自动入账阈值

- 页面分类明确、成功状态明确、金额唯一、新鲜度成立且没有负特征：允许高置信度自动入账。
- 截图结果与近期支付通知的平台、金额和时间一致：提升为高置信度，但仍执行去重。
- 只有 OCR 文字、缺少页面结构或交易时间：进入待确认。
- 同一页面存在多个金额、多个交易条目或字段冲突：进入待确认。
- 无障碍节点树与同一页面截图属于同一个信息来源，不能当作两份独立证据。
- 官方账单匹配成功后，记录才标记为“已对账”。

### 4.4 时间戳保存

每个截图识别事件分别保存：

- `screenshotCapturedAt`：优先取 MediaStore 可用的截图生成时间。
- `detectedAt`：应用发现并开始处理的系统时间。
- `pageTransactionAt`：从页面中识别出的交易时间，可为空。
- `occurredAt`：最终用于账本的交易时间。
- `timeSource`：`PAGE_EXACT`、`NOTIFICATION_MATCHED`、`SCREENSHOT_ESTIMATED`、`STATEMENT_VERIFIED` 或 `USER_CONFIRMED`。

若页面没有交易时间，只能暂时以截图时间作为估计值，并明确标记 `SCREENSHOT_ESTIMATED`；官方账单导入后用真实交易时间校正，同时保留审计记录。用户删除截图后仍保留上述时间戳、规则版本和不可逆内容指纹，但不保留图片或完整 OCR 文本。

## 5. 识别后的交互与截图删除

- 应用不显示常驻浮钮。识别完成后才创建一次性 `TYPE_APPLICATION_OVERLAY`，约 8 至 10 秒后自动移除。
- 高置信度文案为“已录入 ¥xx.xx · 标签”，操作包括“修改”“删除截图”“保留”。
- 非高置信度文案为“检测到疑似支付截图”，操作包括“确认录入”“忽略”；不得显示“已录入”。
- 支付应用阻止第三方浮层、用户未授予浮层权限或后台窗口展示失败时，降级为高优先级通知。
- 标签浮层必须在截图和 OCR 完成后出现，避免自身内容进入截图。
- 点击“删除截图”后调用 `MediaStore.createDeleteRequest()`；由 Android/HyperOS 显示系统确认。应用不得静默删除非本应用创建的截图。
- 只有账目成功持久化后才提供删除入口；识别失败或用户取消时默认保留截图。

## 6. 账本、预算与对账

核心数据：

- `Transaction`：UUID、类型、金额、币种、交易时间、商户、平台、标签、状态、置信度、外部流水号、去重指纹及创建/修改时间。
- `TransactionEvidence`：来源、截图时间字段、规则版本、匹配特征、置信度和对账状态，不保存原始截图。
- `Tag`：名称、颜色、排序和启用状态；不包含预算额度。
- `MonthlyBudget`：年月与总预算；每月只有一个生效预算。
- `ImportBatch`：平台、文件哈希、导入时间、统计结果和撤销状态。
- `AuditEvent`：自动识别、人工修改、账单匹配、时间校正和冲销记录。

预算规则：

- 固定日预算等于当月总预算除以当月自然日数。
- 当日预算结余等于固定日预算减去当日净支出。
- 净支出等于支出减退款；普通收入不抵扣预算，另行展示现金流。
- 标签仅统计金额、占比和趋势，不设置分类预算，也不改变日预算。

账单对账：

- 用户通过系统文件选择器导入自行取得的微信或支付宝官方账单。
- 流水号优先精确匹配；无流水号时按平台、方向、金额、规范化商户和时间窗口匹配。
- 唯一匹配时补充字段并标记已对账；缺失记录补录；多候选和冲突进入对账页。
- 同一文件通过文件哈希防止重复导入；整个导入批次可撤销。
- 账单记录可以校正截图估算时间，但不得无审计记录地覆盖用户手工修改。

## 7. 诊断日志与 GitHub Issue

默认日志必须脱敏且有容量上限，只记录定位兼容性问题所需的信息：

- 应用版本、设备型号、Android/HyperOS 版本和权限状态。
- 截图发现方式、MediaStore 查询阶段、耗时和错误码。
- 图片尺寸、方向及不可逆哈希；不记录图片路径中的用户信息。
- OCR 引擎版本、耗时、文本块数量，不记录完整 OCR 文本。
- 页面规则版本、命中的特征 ID、缺失特征 ID、负特征 ID 和最终分数。
- 自动入账、待确认、去重或拒绝的决策原因。

日志在本地使用固定大小环形缓冲区，默认保存不超过 7 天。用户通过“导出诊断包”主动生成 JSON/ZIP；导出前提供内容预览和隐私提示。应用没有网络权限，不能自动上传日志。

GitHub 仓库应提供 Issue 模板，要求用户：

- 先附加脱敏诊断包，并填写机型、系统版本、微信/支付宝版本和复现步骤。
- 不要直接上传含真实姓名、头像、商户、金额、订单号或二维码的截图。
- 识别规则确需图片时，优先提供人工构造或充分打码的正样本和负样本。
- 如果公开 Issue 无法安全脱敏，只提交规则命中信息，不提交原图。

应用可提供“生成识别样本”工具：用户显式选择图片，在本地预览并遮盖头像、姓名、订单号、二维码和非必要金额区域，再导出。该工具不得默认包含原始图片。

## 8. 图片样本要求

实现页面特征规则前，需要用户提供脱敏测试图片。每个平台尽量包括：

- 支付成功页：小程序、扫码支付、转账、红包/收款等实际会使用的入口。
- 收款成功和退款成功页。
- 深色/浅色模式、不同字体大小和不同屏幕缩放。
- 带交易时间和不带交易时间的页面。
- 负样本：账单列表、月度账单、交易详情、聊天中的支付截图、分享海报和图片预览。
- 容易混淆的失败、处理中、待支付和取消页面。

样本应先去除或替换真实姓名、头像、二维码、条码、订单号和商户隐私。金额可以保留合成值，以便验证金额定位。样本不得提交到公开仓库；仓库只保存合成或彻底脱敏的测试夹具。

## 9. 测试与验收

- 单元测试覆盖金额格式、多个金额、退款、收入、时间解析、跨日、重复截图和哈希去重。
- 分类测试必须包含真实结构的脱敏正样本及数量不少于正样本的负样本。
- 重点验证含“支付成功”文字的聊天截图、账单分享图和历史详情页不会自动入账。
- 验证无页面时间、旧交易时间、通知不匹配时不会错误提升为高置信度。
- 测试 MediaStore 图片尚未写完、重复变更事件、云相册回写、图片编辑和截图目录变化。
- 测试完整照片权限、部分照片权限、拒绝权限和权限被撤销后的降级行为。
- 测试浮层允许、拒绝、被支付应用隐藏及通知降级；界面不能遮挡支付密码或支付确认控件。
- 测试 Android API 26、33 和建项时最新稳定 API；主要真机矩阵记录小米机型、HyperOS/Android 版本及 SystemUI 特征。
- 所有 OCR、识别、记账、导入和导出功能在飞行模式下必须可用。

验收标准：

- 截图后无需分享即可触发识别；失败时可用快捷磁贴重试。
- 高置信度支付成功页只生成一条正确账目。
- 账单、聊天和分享截图不会自动入账。
- 截图生成时间、页面交易时间及其来源可审计。
- 用户删除截图后，账目和脱敏证据仍存在，但设备上不保留应用复制的原图。
- 规则失效时可以通过脱敏诊断包和样本稳定复现，并通过新增回归测试修复。

## 10. 实施顺序与 AI 操作重点

1. 创建基础 Android 工程、加密 Room、手工收支、标签和月/日预算。
2. 实现截图检测接口、HyperOS 通知触发、MediaStore 后备和快捷磁贴。
3. 收集脱敏正负样本，先完成页面分类器及回归测试，再实现字段提取。
4. 实现置信度、新鲜度、时间来源和幂等入账规则。
5. 实现一次性结果浮层、通知降级和系统确认删除。
6. 实现官方账单导入、冲突处理、批次撤销和对账。
7. 实现 CSV/XLSX 月报、诊断包、Issue 模板和安全文档。
8. 在目标 HyperOS 设备完成端到端测试并锁定首批适配配置。

后续 AI 必须遵守：

- 不因单个关键词或 OCR 结果直接自动入账。
- 不保存原始截图、完整通知正文或完整 OCR 文本，除非用户对单个诊断样本明确选择导出。
- 不自行增加联网、无障碍持续扫描、UsageStats、录屏、短信读取或静默删除能力。
- 不将真实支付图片、账单、签名密钥或数据库提交到 Git。
- 修改识别规则时必须同时提交负样本回归测试，优先防止误记账而非追求表面识别率。
- 依赖或系统行为不确定时，先在目标 HyperOS 真机验证并记录结果，不能把推测写成已支持能力。
