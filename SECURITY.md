# Security Policy

## 报告安全问题

请优先使用 GitHub 的私密安全报告功能，不要在公开 Issue 中披露漏洞细节、真实支付截图、账单、日志或个人信息：

https://github.com/VibrateZ/android-vlm-ledger/security/advisories/new

报告中请说明受影响版本、复现条件、潜在影响和可行的缓解方式。提交任何附件前，请移除姓名、头像、二维码、订单号、金额、商户及设备标识等敏感内容。

## 支持范围

项目尚处开发阶段，没有已发布的受支持版本。计划支持通用 Android API 26+；小米/HyperOS 设备作为兼容性测试设备之一，不把厂商私有接口或固定截图路径作为安全前提。首个版本发布后，本节将记录具体 Android API 范围、安全更新策略和已验证设备矩阵。

## 云端 VLM 与数据边界

- 云端识别只有在用户明确启用并配置服务后才可运行。应用只把当前待处理的截图通过 HTTPS 发送到用户配置的 VLM 服务，不上传账本数据库、导入账单、日志、源码、密钥或其他未授权数据。
- 原图从 MediaStore/文件 URI 以输入流按张读取和发送；默认不复制。用户明确开启“应用内保留原图副本”后，仅在成功入账时复制到应用私有目录。请求完成后释放流和临时内存。VLM 服务应承诺并配置为处理完成后立即删除原图，不得默认持久化或用于训练；客户端无法替任意第三方端点验证实际留存行为。
- 应用只保存账本、内容哈希、交易指纹、URI、时间来源、模型/规则版本、特征 ID 和审计信息，不保存原始图片或完整 OCR 文本。任何服务端留存、跨境传输、训练使用和费用政策均由用户在启用服务前确认。
- 生产端点必须使用 HTTPS，并校验证书链；禁止为了绕过错误而降级到明文 HTTP。仅允许在调试环境使用明确标记的本地 Mock 端点。

## API Key 与配置

- `baseUrl`、模型、超时等配置通过表单填写；界面不提供原始请求 JSON 或任意 Header 编辑。
- 用户提供的 API Key 只用于请求已配置的 VLM 服务，输入和展示时掩码，使用 Android Keystore 加密后保存。不得写入源码、Git、普通明文 DataStore、命令行参数、日志、崩溃报告、诊断包或分析事件。
- 日志、错误提示和导出内容必须对 URL 中的凭据、`Authorization` 头和响应中的敏感字段做脱敏。网络失败重试不得把密钥或完整请求体写入日志。
- 应用不得把 API Key 当作可由混淆或 APK 加密永久隐藏的秘密；用户应能撤销、替换和清除本地密钥。

## Android 权限与删除

- Android 13+（API 33+）按需申请 `READ_MEDIA_IMAGES`；Android 12 及以下（API 26-32）申请 `READ_EXTERNAL_STORAGE`。Android 14+（API 34+）还需识别 `READ_MEDIA_VISUAL_USER_SELECTED` 的部分照片授权。只在用户启用截图识别时请求，并说明该权限技术上可读取的范围。
- 后台自动处理独立且默认关闭；只有云端识别、完整照片权限和有效 VLM 配置同时成立时才安排带联网约束的 WorkManager。Android 13+ 通知权限被拒绝时仍保存待确认结果，但无法主动通知用户。
- 用户拒绝、在 Android 14+ 只授予部分照片或撤销权限时，自动发现应关闭并提供手动分享/选择文件或手工记账等降级路径；不得借助 Root、无障碍、UsageStats、录屏或其他高权限绕过系统限制。
- 删除系统截图按 API 级别处理：API 30+ 使用 `MediaStore.createDeleteRequest()` 显示系统确认；API 29 捕获 `RecoverableSecurityException` 并启动其用户确认；API 26-28 仅在用户点击删除且授予 `WRITE_EXTERNAL_STORAGE` 后执行。应用不得静默删除非自身创建的图片，取消、拒绝或失败时默认保留原图。

## 日志、依赖与发布

- 默认日志采用固定容量和保留期限，只记录定位问题所需的版本、权限状态、耗时、错误码、图片尺寸/方向、不可逆哈希、特征 ID 和决策原因；不记录原图、完整 OCR、通知正文、商户、金额或密钥。
- 诊断包只能由用户主动导出，导出前预览并再次脱敏。测试和公开样本使用合成数据或彻底脱敏图片。
- 生产依赖固定版本，并检查来源、许可证、传递依赖和已知漏洞；不得引入广告、遥测、动态代码下载或额外敏感权限。网络库只能服务于用户明确启用的 VLM 请求。
- 发布前至少验证 Android API 26、33 和最新稳定 API 的权限、MediaStore、网络错误处理、Keystore 和系统删除确认；同时在至少一台主流/AOSP 设备和一台 HyperOS 设备上回归。无法实机验证的行为须标为待验证。

## 责任边界

自动识别结果只是建议，最终入账由本地规则和用户确认决定。涉及重要财务判断时，应以支付平台或银行记录为准。Root、恶意输入法、系统失陷或用户主动授予其他恶意应用高权限不在本项目的保护范围内。
